apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: olehalv

resources:
  - deployment.yaml
  - service.yaml
  - http-route.yaml

configMapGenerator:
  - name: codeserver-config
    namespace: olehalv
    literals:
      - CODESERVER_PORT_NUMBER=8080
